delete:
  handler: app/src/sensors/main/delete.handler
  iamRoleStatements:
    - Effect: 'Allow'
      Action:
        - dynamodb:DeleteItem
      Resource: arn:aws:dynamodb:${self:provider.region}:*:table/${self:provider.environment.DYNAMODB_TABLE}        
  package:
    individually: true
    exclude:
      - app/src/**
    include:
      - app/src/sensors/main/delete.js
      - app/src/sensors/model/delete.js
      - app/src/sensors/controller/delete.js
      - app/helper/dynamodb_client/index.js
  events:
    - http:
        path: /api/v1/events/sensors
        method: delete
        cors:
          origins: 
            - "*"
          headers:
            - Content-Type
            - Authorization
        authorizer:
          name: authorize
          resultTtlInSeconds: 30
          identitySource: method.request.header.Authorization
          type: request
