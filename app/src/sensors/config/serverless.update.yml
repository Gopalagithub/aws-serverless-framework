update:
  handler: app/src/sensors/main/update.handler
  iamRoleStatementsName: permitOnlyUpdate #optional custom role name setting instead of the default generated one 
  iamRoleStatements:
    - Effect: "Allow"        
      Action:
        - dynamodb:UpdateItem
      Resource: "arn:aws:dynamodb:${self:provider.region}:*:table/${self:provider.environment.DYNAMODB_TABLE}"
  package:
    individually: true
    exclude:
      - app/src/**
    include:
      - app/src/sensors/main/update.js
      - app/src/sensors/model/update.js
      - app/src/sensors/controller/update.js
      - app/helper/dynamodb_client/index.js
  events:
    - http:
        path: /api/v1/events/sensors
        method: put
        cors:
          origins: 
            - "*"
          headers:
            - Content-Type
            - Authorization
        authorizer:
          name: authorize
          resultTtlInSeconds: 30
          identitySource: method.request.header.Authorization
          type: request
